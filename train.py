from helper import tokenize, get_embedding_table, get_embeddings, get_positional_encoding, multi_head_attention, ffn, layer_norm, linear
import numpy as np

def preprocess(text, vocab, embedding):
    pass
